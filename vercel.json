{
  "projects": {
    "root": {
      "src": "client",
      "buildCommand": "npm install && npm run build",
      "outputDirectory": "client/build"
    },
    "api": {
      "src": "api",
      "functions": "api"
    }
  }
}
